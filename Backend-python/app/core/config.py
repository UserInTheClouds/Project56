import os

# Configuration settings

class Settings:
    PROJECT_NAME: str = "Project56"
    # Optional environment variable
    # API_KEY: str = os.getenv("API_KEY", "default-key")

settings = Settings()
